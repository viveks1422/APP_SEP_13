<%= simple_form_for @event, url: wizard_path, :method => :put do |f|%>

<%= render "event_errors" %>
	<article>
		<label class="amount">
		  <span>Amount: $0.99</span>
		</label>
	</article>

	<script src="https://checkout.stripe.com/v2/checkout.js" class="stripe-button"
	  data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
	  data-description="A new Birmingham Event!"
	  data-amount="99"></script>

	or <%= link_to "Previous step", previous_wizard_path %>
<% end %>